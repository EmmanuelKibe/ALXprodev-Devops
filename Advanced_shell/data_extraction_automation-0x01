#!/bin/bash

# Extract values using jq
name=$(jq -r '.name' data.json)
height=$(jq -r '.height' data.json)
weight=$(jq -r '.weight' data.json)
type=$(jq -r '.types[0].type.name' data.json)

# Convert units using awk (height → meters, weight → kg)
formatted_height=$(echo "$height" | awk '{ printf "%.1f", $1/10 }')
formatted_weight=$(echo "$weight" | awk '{ printf "%.1f", $1/10 }')

# Capitalize first letter of type using sed
type=$(echo "$type" | sed 's/.*/\u&/')

# Output the required formatted string
echo "$name is of type $type, weighs ${formatted_weight}kg, and is ${formatted_height}m tall."
