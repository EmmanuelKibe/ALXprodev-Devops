#!/bin/bash

# Extract fields using jq
name=$(jq -r '.name' data.json)
height=$(jq -r '.height' data.json)
weight=$(jq -r '.weight' data.json)
type=$(jq -r '.types[0].type.name' data.json)

# Convert height (decimeters → meters)
formatted_height=$(echo "$height / 10" | bc -l | sed 's/^\./0./')

# Convert weight (hectograms → kg)
formatted_weight=$(echo "$weight / 10" | bc -l | sed 's/^\./0./')

# Capitalize type using sed
type=$(echo "$type" | sed 's/.*/\u&/')

# Capitalize name using sed
name=$(echo "$name" | sed 's/.*/\u&/')

# Final output
echo "$name is of type $type, weighs ${formatted_weight}kg, and is ${formatted_height}m tall."
